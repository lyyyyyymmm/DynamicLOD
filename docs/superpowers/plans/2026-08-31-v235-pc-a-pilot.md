# v2.3.5 PC-A Full Pilot Execution Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Execute and audit the four-repeat PC-A D1/S2 full pilot under frozen protocol v2.3.5 without changing controller, workload, or analysis parameters.

**Architecture:** Use the existing LAN benchmark page and its `buildD1S2PilotQueue()` path to run 24 shuffled six-method pilot conditions. Use the existing JSON exporter and `lod:analyze` pipeline to classify valid/invalid attempts and produce pilot-only summaries; update project records only with evidence from the new run.

**Tech Stack:** Cesium 1.134 benchmark page, vanilla JavaScript modules, Node.js, Playwright/browser controls, Python analysis scripts, CSV/JSON result artifacts, Markdown project records.

**Spec:** `D:/myCesium202510/Cesium-1.134/Apps/learnMapmost/HANDOFF.md`

## Global Constraints

- Protocol version remains `2.3.5`.
- Keep SSE ladder `[4, 6, 8, 12, 16, 24, 32, 48, 64]`.
- Keep frame budget `33.33 ms`, `windowMs=2000`, `controlIntervalMs=500`, and prediction horizon `1000 ms`.
- Do not change request-pressure thresholds, impulse latch, camera path, dataset, baseline, readiness policy, or statistical plan.
- Use physical device ID `pc-a`, seed `20260823`, dataset `bagAmsterdam`, and scenario `burst`.
- Preserve invalid JSON attempts; retry according to the existing policy and do not pool pilot records with confirmatory statistics.
- Do not start Android or confirmatory collection from this execution.

### Task 1: Verify and execute the PC-A D1/S2 full pilot

**Files:**
- Read: `HANDOFF.md`, `RUNBOOK.md`, `PROJECT_STATE.md`, `experiment-config.mjs`, `lod-benchmark.html`, `lod-benchmark.mjs`
- Create: `results/incoming/*.json` through the existing benchmark exporter

**Interfaces:**
- Consumes: Existing `Run D1/S2 pilot` button and frozen protocol configuration.
- Produces: 24 planned D1/S2 pilot records, with any invalid attempts retained and retried by the existing runner.

- [x] Confirm the server reports protocol `2.3.5` and the page shows the PC-A device ID and seed `20260823`.
- [x] Start `Run D1/S2 pilot` on the normal benchmark page and keep the benchmark tab foregrounded until completion.
- [x] Verify the runner reaches completion and records JSON checkpoints for each planned condition.

### Task 2: Analyze and audit the new pilot records

**Files:**
- Read: `results/incoming/*.json`
- Modify: `results/analysis/STATUS.md`, `results/analysis/all_runs.csv`, and generated analysis tables through `npm.cmd run lod:analyze`

**Interfaces:**
- Consumes: New pilot JSON records and existing analysis schema.
- Produces: Updated pilot status, method summaries, pressure taxonomy, and invalid-run audit counts.

- [x] Run `npm.cmd run lod:analyze` after collection.
- [x] Check every valid record has `preRunReadinessReady=true` and its first measured control row at or after 2,000 ms.
- [x] Check Proposed repeats have nontrivial tiles, bytes, queue peaks, pressure taxonomy, and no low-content collapse.
- [x] Check for cross-method frame-time instability and confirm whether the pilot freeze gate passes.

### Task 3: Update records and verify the handoff outcome

**Files:**
- Modify only if supported by evidence: `PROJECT_STATE.md`, `TODO.md`, `RUNBOOK.md`, `CHANGELOG.md`, `HANDOFF.md`

**Interfaces:**
- Consumes: Analysis status and audited raw records from Task 2.
- Produces: An auditable project-state update that either gates Android pilot readiness or records the blocking failure and next diagnostic action.

- [x] Record that the PC-A pilot passed execution/content stability checks but Android remains gated because pressure-taxonomy repeatability is insufficient for freeze.
- [x] Preserve the sparse-taxonomy gate result and invalid attempts as audit evidence without retuning silently.
- [x] Run the relevant automated tests and health check before reporting completion.
